import { DnButtonDirective } from './dn-button.directive';

describe('DnButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new DnButtonDirective();
    expect(directive).toBeTruthy();
  });
});
